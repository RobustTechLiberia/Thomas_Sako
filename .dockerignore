.git
.gitignore

# Dependencies are installed inside the image.
**/node_modules

# Frontends are built inside the image.
dist
admin/dist
admin/.vite

# Secrets / local runtime data — never ship these.
.env
.env.*
server/.env
server/.env.*
server/uploads
server/_dbinspect.mjs

# Logs and OS cruft
*.log
.DS_Store
Thumbs.db