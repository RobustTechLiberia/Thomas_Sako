export default {
  // eslint-disable-next-line no-undef
  plugins: [require("@tailwindcss/vite")],
  theme: {
    extend: {
      fontFamily: {
        anton: ["Anton", "sans-serif"],
      },
    },
  },
};
